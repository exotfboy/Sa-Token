tasks:
  - name: Start Logto
    init: |
      docker-compose pull
    command: |
      docker compose up -d && cd sa-token-demo/sa-token-demo-oauth2-server && mvn spring-boot:run
    env:
      TAG: prerelease


ports:
  - name: OAuthServer
    port: 8081
    visibility: public
